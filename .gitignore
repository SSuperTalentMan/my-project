# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# ============ 本项目新增 ============
# 环境变量（可能含密钥）
.env
.env.local

# 运行期产物
logs/
data/reports/
data/*.db
data/*.db.lock
data/milvus_demo.db*
# Milvus Lite 的数据目录（MILVUS_URI=./data/milvus/commercepivot.db 实际是目录）
data/milvus/

# 本地模型权重
models/

# 生成的数据集：保留 knowledge 语料，其余 CSV 属可再生产物
data/sample_*.csv

# 工具与编辑器
.idea/
.vscode/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ============ 不随仓库发布 ============
# AI 工作记忆 / 开发过程日志 / 调试截图（本地留存，不入库）
.workbuddy/

# 个人求职材料（含简历口径与真实数据规模的对照说明），禁止公开
docs/简历_*.md
